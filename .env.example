# FutDB API key. Create a free key at futdb.app.
# The real .env is gitignored and must never be committed.
# Used once by the seed loader. The app works fully offline after the initial pull.
FUTDB_API_KEY=

# Local SQLite database file. Gitignored.
DATABASE_URL="file:./prisma/dev.db"
